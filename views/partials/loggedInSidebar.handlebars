   	<link rel="stylesheet" href="/assets/css/coin-styles.css">

   
    <aside class="sidebar-menu bg-dark" data-bs-theme="dark">
        <div class="justify-content-center ms-2">
            <h5 class="account-title display-6 text-light px-5 pt-5">Account: <span class="span-user" >{{username}}</span> </h5>
            <p class="balance-title h2 text-light px-5 pt-1">Credit Balance: $<span class="text-success">{{balance}}</p>
        </div>
        {{!-- <div>
            <form class="bet-form px-5">
                <input class="bet-input row mt-3 ms-1 form-control" id="bet-input"  type="text" placeholder="Enter Bet Amount">
                <button class="btn btn-success row mt-3 ms-1 form-control" id="bet-btn"  type="submit">Place Bet</button>
            </form>
        </div> --}}
    </aside>
